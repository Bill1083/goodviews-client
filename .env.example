# Copy to .env and fill in. Never commit .env.

# Supabase — Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Flask backend URL (http://localhost:5000 for local dev)
VITE_API_BASE_URL=http://localhost:5000
